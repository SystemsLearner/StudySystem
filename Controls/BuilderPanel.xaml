<UserControl x:Class="StudySystem.Controls.BuilderPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StudySystem"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Root">
    <Border>
        <Grid x:Name="BuilderPanelObject" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <!-- Style="{StaticResource EditorLabelStyle}" -->
                <!-- Style="{StaticResource EditorTextBoxStyle}" -->
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding LabelText, ElementName=Root}" Grid.Row="0"
                       Style="{StaticResource EditorLabelStyle}"/>
            <!-- Text="{Binding InputText, ElementName=Root, UpdateSourceTrigger=PropertyChanged}" -->
            <TextBox x:Name="InputTextBox"
                     Text="{Binding InputText, ElementName=Root}" Grid.Row="1"
                     Style="{StaticResource EditorTextBoxStyle}"/>
        </Grid>
    </Border>
</UserControl>
