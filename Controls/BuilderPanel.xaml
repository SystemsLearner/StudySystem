<UserControl x:Class="StudySystem.Controls.BuilderPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StudySystem"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="Root">
    <Border BorderThickness="0">
        <Grid x:Name="BuilderPanelObject" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding LabelText, ElementName=Root}" Grid.Row="0"
                       Style="{StaticResource EditorLabelStyle}" Foreground="#C6C6C6"/>
            <Border Grid.Row="1" CornerRadius="4"
                    Background="#C6C6C6" BorderBrush="#C6C6C6" BorderThickness="1"
                    Padding="1" Margin="0,0,0,0">
                <TextBox x:Name="InputTextBox" Foreground="#C6C6C6"
                         Text="{Binding InputText, ElementName=Root, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource EditorTextBoxStyle}" BorderThickness="0"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
